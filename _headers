# HTML pages: always revalidate so the latest deploy shows immediately (no stale)
/*
  Cache-Control: public, max-age=0, must-revalidate

# Static assets: long cache (versioned via ?v= or stable filenames)
/assets/*
  Cache-Control: public, max-age=31536000, immutable
